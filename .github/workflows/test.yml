name: Test scripts

on:
  push:
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: test
        run: |
          ./scripts/build.sh
          ./scripts/test.sh
        timeout-minutes: 1
